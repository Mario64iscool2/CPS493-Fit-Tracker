{
  "scripts": {
    "dev": "nodemon server/index.js --watch server/",
    "build": "cd client; npm i; npm run build; cd ..; npm i",
    "preview": "vite preview",
    "build-only": "vite build",
    "type-check": "vue-tsc --build --force",
    "css-build": "node-sass --omit-source-map-url src/sass/mystyles.scss src/assets/mystyles.css",
    "css-watch": "npm run css-build -- --watch",
    "start": "node server/index.js"
  },
  "dependencies": {
    "config": "^3.3.11",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
